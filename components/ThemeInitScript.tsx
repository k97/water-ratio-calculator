'use client';

// This component is now just a placeholder since we're moving the script to layout.tsx
// We'll keep it for now to avoid breaking imports, but it doesn't do anything
export default function ThemeInitScript() {
  return null;
}
